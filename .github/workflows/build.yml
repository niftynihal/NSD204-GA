# This is a YAML file

on: push
jobs: 
    second-job:
      runs-on : ubuntu-latest
      steps : 
            - run : node --version
            - run : npm  --version
            - run : node index.js
